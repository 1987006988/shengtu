# A2 主线任务层复盘

## 1. 本轮复盘对象

- `worldbible/08_story/90_a2_mainline_task_package.md`
- `worldbible/08_story/91_a2_mainline_mq.md`
- `worldbible/08_story/92_a2_mainline_journal.md`
- `worldbible/08_story/93_a2_mainline_codex.md`
- `worldbible/08_story/94_a2_mainline_state_trigger_table.md`
- `worldbible/08_story/83_a2_mainline_prose_ch04.md`
- `worldbible/08_story/84_a2_mainline_prose_ch05.md`
- `worldbible/08_story/85_a2_mainline_prose_ch06.md`

## 2. 范围与门槛检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 是否以 `89_a2_mainline_prose_review.md` 为前置门槛 | `PASS` | `90` 已明确写死此前置条件。 |
| 是否只锁定 `A2 / CH04–CH06` | `PASS` | `90–94` 全部停在 `CH04–CH06`。 |
| 是否不含支线、不含 `CH07+`、不含旧 narrative-lab 回收 | `PASS` | 本轮主线任务层未打开这三类入口。 |

## 3. 上游护栏检查

| 上游文件 | 结果 | 说明 |
| --- | --- | --- |
| `15` | `PASS` | 所有任务节点只落在断引栈道、鹿门集、风窖台的现实承压链。 |
| `16` | `PASS` | 任务层只写道路政治、征调分账与地区级代价，不前置历史翻面或终局比较。 |
| `21` | `PASS` | 只调用 `沈 / 柳 / 顾 / 祁 / 桑` 的 `A2` 接口。 |
| `22` | `PASS` | 未提前抬 `宁折羽 / 穆问碑`，角色顺序稳定。 |
| `54 / 55` | `PASS` | `MQ`、`Journal`、`State Trigger` 都回指 `CH04-S01–CH06-S04` 已落库章场。 |
| `56` | `PASS` | `Codex` 与 `Journal` 都只释放 `A2` 允许知道的信息。 |
| `57 / 58` | `PASS` | 角色推进与状态触发都停在 `A2` 允许的增量内。 |

## 4. 与已写出正文的一致性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `91` 是否与 `83–85` 的场景推进一致 | `PASS` | 六个 `MQ` 节点都能回锚到已落库正文场段。 |
| `92` 是否只记录正文中已知、且会改变行动判断的信息 | `PASS` | 没有提前把碑林、宁折羽或终局比较写入日志。 |
| `93` 是否只做 `A2` 现象级 `Codex` | `PASS` | 没有越界成灰线与古战总说明。 |
| `94` 是否只锁 `A2` 状态，并把 `SQ-SCOPE-02` 放在 `95 PASS` 之后 | `PASS` | 状态表终点停在 `V1-A2-CLOSED`，再后才允许支线正式化。 |

## 5. 复盘结论

本轮 `V1 / A2 / CH04–CH06` 主线任务层复盘结果为：`PASS`。

这代表：

1. `A2` 主线任务层现在可以作为已通过的受控包存在；
2. 下一步才允许解冻 `SQ-SCOPE-02《断路前的空名册》`；
3. 当前仍不适合把控制链改写成超出已审边界的任务层全开口径。

## 6. 一句话口径

> 主线任务层通过，只代表 `A2 / CH04–CH06` 这一段已经能稳定承接正文与蓝图，不代表 `V1` 或 `Book1` 已开放完毕。
