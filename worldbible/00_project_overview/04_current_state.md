# 04 当前状态

## 1. 文件用途
本页只记录仓库当前真实状态，不再保留旧 batch 或旧 narrative-lab 作为默认入口。

## 2. 项目一句话定位
《升途》/ `Ascendant Path` 是一个宇宙级中式玄幻 CRPG 世界观与叙事母本仓库。第一部《升途：封绝之地》的当前真实施工形态是：

> `Book1` 开放范围内的生产执行模式

## 3. 当前真实阶段
- `Book1` 主线正文已开放，但当前重点不是继续证明能否开放，而是在现役资产上就地生产化修订。
- `Book1` 主线任务层（`MQ / Journal / Codex / State Trigger`）已开放，但当前重点同样是生产化修订，不是重开验证链。
- `Book1` 支线任务层仍只按矩阵开放。
- `Item / System feedback` 继续冻结。
- `157–162` 已作为生产控制、正式化支线与总复盘文件入库。

## 4. 已经闭环的资产
- `15 / 16 / 21 / 22 / 54 / 55 / 56 / 57 / 58`
- `A1 / CH01–CH03`
- `A2 / CH04–CH06`
- `A3 / CH07–CH09` 与 `A4 / CH10–CH12`
- `A5 / CH13–CH15` 与 `A6 / CH16–CH18`
- `V1 / V2 / V3`
- `book1_*` 全书审计资产

## 5. 当前允许做什么
- 按 `157_book1_production_manifest.md` 与 `158_book1_open_scope_work_queue.md` 的队列，就地修订现役主线正文与主线任务层。
- 在支线矩阵 `OPEN` 范围内正式化 `SQ-SCOPE-01 / SQ-SCOPE-02`。
- 保留 `79 / 80 / 96 / 97` 作为试点证据链。
- 继续冻结 `Item / System feedback`。

## 6. 当前明确禁止
- 把当前状态写成“全面开放”或“全面任务层已开放”。
- 把“支线按矩阵开放”写成“所有支线都开放”。
- 在没有授权前私开矩阵外支线。
- 在没有独立试点与复盘前开启 `Item / System feedback`。
- 恢复旧 batch 或 narrative-lab 作为当前默认施工链。

## 7. 当前默认下一步
> 当前默认下一步不是再新增一套验证链，而是按 `157–159` 的生产控制文件推进现役资产的就地修订与正式化。
> 先消耗生产队列，再用 `162` 做一次 open-scope 生产复盘。

