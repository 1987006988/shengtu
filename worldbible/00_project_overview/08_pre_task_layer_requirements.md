# 08 进入任务层前置条件清单

## 1. 文档用途
本页记录“进入任务层前”的前置条件是否满足，以及当前哪些任务层已经正式开放，哪些仍未通过审计。

## 2. 当前总口径
- `Book1 / CH01–CH18` 主线正文已通过全书集成复盘。
- `Book1` 主线任务层（`MQ / Journal / Codex / State Trigger`）已通过全书集成复盘。
- `Book1` 支线任务层当前只按矩阵开放。
- `Item / System feedback` 仍未通过独立试点与审计。

## 3. 已满足的前置条件
1. `15 / 16 / 21 / 22 / 51–58` 已作为上游母表落库并持续有效。
2. `A1 / CH01–CH03` 主线正文与主线任务层已通过 `72 / 78`。
3. `SQ-SCOPE-01` 已通过 `80`。
4. `A2 / CH04–CH06` 主线正文与主线任务层已通过 `89 / 95`。
5. `SQ-SCOPE-02` 已通过 `97`。
6. `V1 / CH01–CH06` 已通过 `98`。
7. `A3 / CH07–CH09` 与 `A4 / CH10–CH12` 已通过 `106 / 112 / 120 / 126 / 127`。
8. `A5 / CH13–CH15` 与 `A6 / CH16–CH18` 已通过 `135 / 141 / 149 / 155 / 156`。
9. `Book1` 全书揭示、角色、世界状态、主线正文、主线任务层与支线矩阵审计均已通过。
10. 控制层文件已经同步到 `Book1 主线开放 + 支线按矩阵开放 + Item/System feedback 冻结` 口径。

## 4. 当前已经开放的任务层范围
- `Book1` 主线 `MQ`
- `Book1` 主线 `Journal`
- `Book1` 主线 `Codex`
- `Book1` 主线 `State Trigger`
- 支线矩阵中标记为 `OPEN` 的已审范围

## 5. 当前仍未开放的范围
- 支线矩阵中标记为 `FROZEN` 的所有范围
- `Item`
- `System feedback`

## 6. 当前允许继续做什么
- 在已开放的主线任务层范围内继续施工或回修。
- 在支线矩阵 `OPEN` 范围内继续施工或回修。
- 为矩阵外支线补新的受控试点。
- 为 `Item / System feedback` 补独立试点与审计。

## 7. 当前仍不允许做什么
- 把“主线任务层开放”偷换成“全面任务层已开放”。
- 把“支线按矩阵开放”偷换成“全量支线开放”。
- 在没有试点与复盘的前提下直接生成 `Item / System feedback`。

## 8. 当前阶段结论
> 本页原先承担的是“能否进入任务层”的门槛说明。
> 现在该门槛对 `Book1` 主线任务层已经完成；
> 仍未完成的，只剩支线矩阵外范围与 `Item / System feedback` 的独立验证。
