# 08 进入任务层前置条件清单

## 1. 文档用途

本文档用于明确：在默认进入任务层之前，项目至少还需要满足哪些前置条件，以及当前哪些条件已满足、哪些仍未满足。

本文件是进入任务层前的执行清单，不承载任务设计本身。

## 2. 当前总口径

- `15 / 16 / 21 / 22 / 51–58` 文件组已落库；
- `50–58` 已形成第一部蓝图闭环；
- 蓝图一致性审计已通过；
- 当前允许进入的不是全面任务层，而是 `Act I` 最小试点。

## 3. 哪些条件现在已经满足

### 3.1 白鹿原与第一部绑定文件已落库

- `15_bailuyuan_region_bible.md`
- `16_book1_binding_boundary.md`

### 3.2 角色总控文件已落库

- `21_party_story_function_bible.md`
- `22_character_progression_master.md`

### 3.3 第一部蓝图文件组已落库

- `51_book1_master_outline.md`
- `52_book1_volume_structure.md`
- `53_book1_act_structure.md`
- `54_book1_chapter_blueprint.md`
- `55_book1_scene_blueprint.md`
- `56_book1_reveal_foreshadow_table.md`
- `57_book1_character_progression_table.md`
- `58_book1_world_state_table.md`

### 3.4 蓝图闭环粒度已达到可试点评审级

- `54_book1_chapter_blueprint.md` 已写到 `CH18`；
- `55_book1_scene_blueprint.md` 已写到 `CH18-S04`；
- `56 / 57 / 58` 已对揭示、角色与世界状态形成闭环表；
- `execution_plan_rebuild.md` 已限定试点只能是 `Act I` 主线最小试点、最小 `Journal / Codex` 样例与 `1–2` 条支线试点。

### 3.5 试点范围、交付件与禁止事项已落库

- `worldbible/09_reference/act1_minimal_pilot_package.md` 已锁定 `CH01–CH02` 的最小试点范围；
- 最小主线试点、最小 `Journal / Codex` 样例与禁止事项已正式写入；
- `rebuild_execution_state.md` 已同步到试点口径。

## 4. 此前未满足项的当前状态

1. `15 / 16 / 21 / 22 / 51–58` 与控制层文件之间的跨文件一致性审计已通过；
2. `Act I` 最小试点范围已锁定为 `CH01–CH02`；
3. 最小任务 schema、最小交付件与禁止事项已写入 `act1_minimal_pilot_package.md`；
4. `04_current_state.md`、`session_handoff.md`、`rebuild_execution_state.md` 与 `06_stage_gates.md` 已同步到试点口径；
5. 旧资产已在控制层与执行层被集中降级为历史资产，不再具有默认执行权。

## 5. 现在允许进入什么层级

当前允许进入的，只有：

- `Act I` 最小主线任务化试点；
- `CH01–CH02` 级别的最小试点范围；
- 最小 `Journal / Codex` 样例；
- 至多 `1–2` 条支线试点的范围定义。

## 6. 现在仍然不允许什么

- 全面任务文本工程；
- 恢复旧 `game_narrative_*`、`game_batch*`、`game_phase*`、`final_acceptance*` 体系；
- 一次性大规模生成 MQ / SQ / Journal / Codex / Item / System feedback；
- 让任务层反向改写 `14 / 15 / 16 / 21 / 22 / 51–58` 的上游边界。

## 7. 什么情况下才适合进入试点

只有同时满足以下条件，才适合正式进入最小试点：

1. 蓝图一致性审计 `PASS`；
2. `Act I` 最小试点范围已锁定；
3. `rebuild_execution_state.md` 已写明交付件、禁止事项与下一最小步；
4. 控制层文件已经同步到统一口径；
5. 试点仍明确服务《升途：封绝之地》与白鹿原尺度。

## 8. 当前阶段结论

> 当前已具备进入 `Act I` 最小试点的条件；当前允许推进的，是 `CH01–CH02` 级别的一条最小主线试点与最小 `Journal / Codex` 样例，而不是全面任务文本工程。
