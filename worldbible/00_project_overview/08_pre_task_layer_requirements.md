# 08 进入任务层前置条件清单

## 1. 文件用途
本页记录进入任务层前必须满足的条件，以及当前仍然冻结的范围。

## 2. 当前总口径
- `Book1 / CH01–CH18` 主线正文已通过全书审计。
- `Book1` 主线任务层（`MQ / Journal / Codex / State Trigger`）已通过全书审计。
- `Book1` 支线任务层当前只按矩阵开放。
- `Item / System feedback` 仍未通过独立试点与审计。
- 当前真实阶段是 `Book1` 开放范围内的生产执行模式，不是验证链模式。

## 3. 已满足的前置条件
1. `15 / 16 / 21 / 22 / 54 / 55 / 56 / 57 / 58` 已作为上游母表落库并持续有效。
2. `A1 / CH01–CH03`、`A2 / CH04–CH06`、`A3 / CH07–CH09`、`A4 / CH10–CH12`、`A5 / CH13–CH15`、`A6 / CH16–CH18` 均已闭环。
3. `book1_*` 全书揭示、角色、世界状态、主线正文、主线任务层与支线矩阵审计均已通过。
4. `157–162` 生产控制文件已建立。

## 4. 当前仍冻结的范围
- 矩阵外支线
- `Item`
- `System feedback`

## 5. 当前允许继续做什么
- 在生产队列内继续就地修订主线正文与主线任务层。
- 在支线矩阵 `OPEN` 范围内正式化已审支线。
- 按 `157–159` 做生产控制、工作队列与变更门禁。

## 6. 当前仍不允许做什么
- 把“主线任务层开放”偷换成“全面任务层已开放”。
- 把“支线按矩阵开放”偷换成“全量支线开放”。
- 在没有独立试点与审计的前提下直接生成 `Item / System feedback`。

## 7. 当前阶段结论
> 进入任务层的前置条件已经满足，但当前不是回到验证链，而是进入生产执行模式。
> 下一步应优先消耗生产队列，再做正式化支线与总复盘。

